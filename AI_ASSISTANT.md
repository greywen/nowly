# Nowly AI 助手（第一阶段）

## 使用入口

AI 输入框嵌入桌面前台窗口的顶部导航栏中间，不新增导航高度，不压缩日历、看板和四象限；聊天面板从输入框下方向工作区覆盖展开。

## AI 输入条 UI/UX 基线

自然语言入口采用当前聊天与操作记录两个独立视图；当前请求的所有结果统一进入聊天，不再额外弹出确认或结果面板：

- 顶部导航中的输入条始终是稳定锚点，只有一行：左侧标记、中间单行输入、右侧操作记录与发送；输入为空时发送按钮变为语音输入，处理中变为停止。日期位于左侧、全局工具位于右侧；窄宽度优先隐藏日期区，输入条与工具不得重叠。面板开合不改变工作区布局。
- 输入框获得焦点时展示当前聊天。用户文字和 Nowly 普通答复使用左右错位的浅色消息气泡，只靠位置与底色区分角色，不显示头像、时间或“你 / Nowly”身份标签；查询记录、待确认方案、执行回执、撤销结果、错误及过期状态使用消息流内卡片。
- 当前请求产生的结构化业务内容只在聊天消息流中出现。新建、修改或删除日程/任务时，确认预览就是对应的聊天卡片，不再滑出额外的“变更预览 / 执行回执 / 查询结果 / 状态更新”面板。
- 卡片是一次业务操作的唯一展示实体。批准、取消、更新预览、撤销和状态核实都在对应卡片内完成；操作后原卡片原位更新为已执行、已撤销、已取消、已过期或待核实，失效操作立即禁用，不追加重复回执卡片。
- 变更卡先展示动作、目标和关键差异，编辑控件按需展开；操作区在长内容中保持可见，确认按钮始终表达准确项目数和危险级别。
- 查询结果按一次回复组成记录卡组。日程和任务卡展示来源、关键日期或时间及只读状态，并可打开原有业务编辑器；业务编辑器只负责编辑，不承担助手结果展示。
- “操作记录”保留为独立历史视图，继续提供今天 / 近 3 天 / 近 7 天筛选、详情和撤销；按日期分组后，每次操作只占一条扁平主行，统一显示业务图标、变更标题、数量、状态与操作，不使用纵向轨道或嵌套卡片。历史记录不进入当前聊天。再次点击历史按钮返回此前聊天，聊天内容和待确认卡保持不变。
- 错误和处理中状态尽量归属具体消息或卡片。只有连接未配置等无法归属单次请求的问题使用聊天内系统卡；停止处理也在对应请求位置说明未提交变更。
- 保留模型只提出方案、编辑立即作废旧预览、显式确认后才执行、状态不明时禁止重复操作等安全契约。“好的”等聊天文字永远不能替代卡片按钮确认。
- 当前对话不长期保存；持久化的仍只有既有操作记录。重新打开应用时可在操作记录视图查看历史，但不伪造已经丢弃的普通对话上下文。
- 面板、卡片和状态均即时切换，不使用 `transition`、`animation`、自动滚动或加载旋转，严格遵循根设计规范。
- 继续复用根设计规范的暖白表面、青绿色主操作、4px 间距刻度、15.2px 圆角、预定义阴影、Lucide 图标与焦点环。

验收以 `AssistantDock` 组件测试和 `tests/assistant.spec.ts` 四视口测试为准。必须断言当前请求的结构化业务结果只存在于聊天列表、卡片操作原位更新、操作记录仍为独立视图、切换后聊天不丢失、失效操作不可点击，并且所有视口无页面溢出。视觉复核至少覆盖空态、理解中、澄清、查询卡、可编辑预览、已执行、已撤销、待核实和历史记录。

打开“设置 · 模型设置”，填写 API 地址、准确 Model ID（Azure 为部署名称）和 API Key，
然后选择允许访问的数据范围，点击“检测并保存连接”。未连接时，聊天内系统卡会直接
提供“打开模型设置”入口。无需选择协议或填写独立
API 版本。请求由 Rust 后端的 Rig 0.42 适配器发送。

- 后端优先参考完整接口路径、服务域名/本机端口，再参考模型名称排序；
  失败后继续尝试其他协议和基础路径，最多 8 个候选。
- 检测仅发送固定的 JSON 测试消息，不发送日程、任务或聊天记录；
  首个有效响应成功后保存识别结果，后续业务请求直接使用该结果。
  每个候选含兼容重试最多 15 秒，全部超时约 2 分钟；测试可能产生服务商费用。
- 全部失败时，主警报仅显示“连接失败，请检查 API 地址、Key 和 Model ID 后重试。”
  用户可主动展开已脱敏的协议、地址和 HTTP 错误详情；输入与原有配置保持不变。
  检测只访问所填地址的同源路径，不猜测其他服务域名、不跟随重定向。

- 支持 OpenAI Chat Completions、OpenAI Responses、Azure OpenAI、
  Anthropic 原生 Messages、Gemini GenerateContent 和 Ollama 原生接口。
- Chat Completions / Azure 使用 `max_completion_tokens` 和 JSON 模式；
  仅在 400/422/501 明确表示相关字段不支持时，改用 `max_tokens` 且不带
  JSON 模式重试一次；若只拒绝 JSON 模式，则保留现代输出长度参数。
  单个协议内认证、模型不存在、额度等错误不自动重试；连接检测仍会继续下一候选。
  重试可能额外计费。模型回复的 Markdown JSON 包装会自动去除。
- API 地址填写基础地址（例如 `https://api.example.com/v1`），也可以填写完整的
  `/chat/completions` 地址。Anthropic 填资源根地址或 `/v1/messages`；
  Gemini 填资源根地址或 `/v1beta`；Ollama 填如 `http://127.0.0.1:11434`。
  Azure 填资源根地址或完整部署接口，使用 `api-key` 认证；
  API 版本从地址中的 `api-version` 读取（默认 `2024-10-21`）。
  Azure 新版 `/openai/v1` 会尝试 OpenAI 兼容接口。
  此处不内置任何账号或 Key。
- 远程地址必须 HTTPS；本机 `localhost` / 回环 IP 可用 HTTP 和空 Key。
  本地模型需自行启动推理服务，本应用不加载模型权重。不开放局域网明文 HTTP。
- Key 由 Windows DPAPI 加密保存在本地，接口不返回明文；不会读取 Codex 凭据。
  只有地址完全不变时才复用已保存的 Key；更换地址必须提供新 Key
  （本机无 Key 服务除外），不会将旧 Key 发往新地址。
  主动清除远程 Key 不要求连接检测成功，确保服务不可用时仍能移除凭据。
- 响应最多 1 MiB，错误先脱敏再截断；Gemini Key 使用请求头，不放入 URL。
  被截断、过滤或未正常完成的响应不进入计划解析。只处理最终文本，不执行工具调用。
- 网页开发预览不提供模型连接或凭据保存，使用该功能需要 Tauri 桌面端。
- 文本输入和本次获准查询的数据会发给指定服务商。数据范围默认关闭。
  撤回权限不会撤回已发送的数据；服务商收费和留存策略由其决定。

## 操作方式

1. 输入请求。Enter 发送，Shift+Enter 换行；中文输入法选词不会误发送。
2. 查询以聊天内记录卡展示；有歧义时要求补充，不执行写入。
3. 修改操作以聊天内卡片显示后端生成的预览：实际日期、修改前后差异、删除数量、
   日程重叠、任务联动和未纳入的记录。
4. 可以编辑标题、时间、提醒、任务描述/日期/四象限/所属列/完成状态，
   或取消勾选某个目标。任何编辑都会立即禁用旧计划，必须“更新预览”。
5. 只有点击“确认执行 N 项”才会提交。聊天中的“好的”不是确认按钮。
6. 成功后刷新日历及共享任务工作区，原卡片切换为已执行状态并提供撤销；撤销后仍在原卡片显示结果。

例子：

- “周三上午 8 点提醒我早会”
- “查看这周五的安排”
- “取消这周五的本地安排”
- “把发布检查放到重要不紧急”
- “把设计评审任务移到进行中”

任务截止时间只精确到日期。需要定时提醒时，使用日历事件。
默认新事件持续 1 小时，预览中可修改；未明确要求的提醒不会自动添加。

## 第一阶段边界

| 能力 | 边界 |
| --- | --- |
| 日历查询 | 每次最多 90 天；最多返回 200 项，截断后禁止生成本次写入计划 |
| 本地日程 | 新建、编辑、提醒、删除；重复日程仅能编辑/删除已查询的某一次 |
| 重复日程 | 不创建/修改规则或整系列；暂不修改单次提醒及全天类型 |
| 任务 | 共用既有看板/四象限数据，按原有完成列及视图联动规则处理 |
| 批量写入 | 一份计划 1–20 个唯一目标，同一主业务类型；整体成功或整体回滚 |
| 外部日历 | 可单独授权读取，始终禁止 AI 写入，显示来源及同步时间 |
| 冲突 | 提示本地及已授权外部日历的时间重叠，不自动改期 |
| 预览 | 5 分钟有效；数据/权限变更或进程重启后不能直接执行旧预览 |
| 撤销 | 只应用本次逆向差异，不恢复整库；相关数据有后续修改时拒绝覆盖 |
| 停止 | 停止后不接纳迟到方案；已发出的 HTTP 请求可能仍在结束 |
| 模型请求 | 连接建立最多 10 秒、单次业务请求整体 90 秒；连接检测每候选最多 15 秒、全部约 2 分钟；不跟随重定向，避免把 Key 转发到其他地址 |
| 回执 | 执行响应丢失时按原计划 ID 核实状态，不盲目重复执行 |
| 记录 | 最近 7 天，最多 50 条；在独立操作记录视图按时间范围查看；打开助手/读取配置或生成计划时清理过期记录 |

数据规模过大（任一安全快照表超过 10,000 行）时，预览拒绝执行并引导使用原有编辑器。
版本检查比较保守：其他业务数据变化也可能要求重新生成预览。
记录清理是数据库逻辑删除，不是磁盘取证级安全擦除。

不包含：主题生成、Skill/模块生成、任意 JS/SQL、看板管理、外部邀请/消息、
后台自主行动、多业务流程自动执行。

助手在壁纸、布局编辑、引导和业务弹窗期间隐藏，不抢焦点。
手动日历、看板和四象限功能不依赖 AI 服务。

## 实现与验证

- Rust：严格 action 白名单、真实域校验、嵌套事务预演、
  固定表差异提交、幂等回执、相关数据保护的逆向撤销。
- 连接失败时区分超时与连接失败，并显示 HTTP 状态和服务返回的错误说明；
  该说明会先把 API Key 替换为 `***`，再折叠空白并截断到 300 字符。
- 模型只能查询或提出方案，没有执行工具。输出无效、越权、目标未查询、
  日期溢出或查询截断时拒绝写入。
- React：当前聊天使用类型化消息流、可编辑卡片和原位状态更新；历史保留独立视图；
  同时覆盖立即失效、输入法保护、取消/迟到回复处理及状态核实。时间选择器的键盘编辑立即上报。
- 数据库升级为 23；新增助手独立表和业务修订触发器，不更换已有业务数据。

自动化验证分别覆盖真实 SQLite 和受控 HTTP、前端状态机，以及
1366×768 / 1920×1080 / 2560×1440 / 5120×1440 浏览器操作。浏览器使用明确隔离的 IPC 测试数据，
不能替代真实模型或原生桌面验证。

2026-09-09 当前聊天卡片回归：移除当前请求的独立结果与回执视图，查询记录、
待确认方案、执行、撤销和过期状态统一进入当前聊天并原位更新；操作记录保留独立视图。
前端 73 个文件 / 574 项通过，生产构建通过；助手浏览器测试在 1366×768、
1920×1080、2560×1440、5120×1440 四个尺寸共 36 项通过。
共享 Vite 开发页不具备原生 Tauri IPC，因此真实模型请求与原生事件生命周期仍以桌面版验证为准。

2026-09-08 多协议回归：前端 73 个文件 / 545 项通过（2 workers）；
Rust 424 项通过、1 项既有忽略；助手浏览器测试在 1366×768、
1920×1080、2560×1440、5120×1440 四个尺寸共 20 项通过。
六种协议分别通过真实本机 HTTP 路径、认证及响应解析测试。
独立审查发现的 Azure 输出参数和异常结束状态问题已修复并补回归。
构建产物大于 500 kB 的提示仍存在。

2026-09-08 自动识别回归：前端 73 个文件 / 546 项通过，Rust 全量 436 项通过、
1 项既有忽略；生产构建、Rust 格式和差异检查通过。
构建页面上的四视口助手浏览器测试 20 项通过（临时 Vite preview 端口 1421）；
开发服务器首次运行存在页面加载超时，因此改用构建页面复验。
同时修复浏览器测试中点击日历后未等待日期弹窗出现的竞态。
新增后端用受控 HTTP 验证六种协议的真实路径与鉴权、逐项回退及全失败脱敏，
并覆盖旧配置读取、检测失败不保存、旧 Key 不跨地址复用和并发设置保护。
独立审查提出的 URL/模型名提示优先级问题已补充冲突输入回归并修复。

未在本轮使用真实服务商 Key，也未验证真实模型理解质量、计费、真实外部账号
或 Windows 通知最终送达。创建提醒成功不等于通知已送达。

复验：

```powershell
npm test -- --reporter=dot
npm run build
cargo test --manifest-path src-tauri/Cargo.toml --quiet
cargo fmt --manifest-path src-tauri/Cargo.toml --all --check
npx playwright test tests/assistant.spec.ts tests/nowly-empty-startup.spec.ts tests/no-page-scroll.spec.ts --project=1366x768 --project=1920x1080 --workers=1
git diff --check
```

运行桌面开发版：`npm run tauri dev`。运行会使用当前用户的正常应用数据；
测试与试用前请确认自己的数据备份策略。自动化存储测试使用内存数据库。
